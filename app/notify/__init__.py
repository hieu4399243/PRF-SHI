"""
Nhắc lịch & thông báo đẩy.

    push.py     gửi push qua Expo Push Service (miễn phí, không cần API key)
    worker.py   quét lịch sắp tới -> bắn nhắc (--once / --watch / --test)

Chạy worker:  python -m app.notify.worker --watch
"""
